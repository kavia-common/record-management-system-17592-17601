# Example environment configuration for backend_api
# Copy to .env and adjust values for deployment.

# SQLAlchemy database URL; defaults to sqlite:///./app.db if not set in code.
# For production, consider using a proper environment variable and secrets management.
# SQLALCHEMY_DATABASE_URL=sqlite:///./app.db

# CORS origins (comma-separated). If omitted, defaults to allow all in code for development.
# CORS_ALLOW_ORIGINS=http://localhost:3000
